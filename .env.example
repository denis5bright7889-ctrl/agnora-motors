# Copy this file to .env.local and fill in your own values.
# NEVER commit .env.local — it is already in .gitignore.

# ── Database (Neon PostgreSQL) ──────────────────────────────
# Get from: https://neon.tech → your project → Connection string
DATABASE_URL=postgresql://user:password@ep-xxx.region.aws.neon.tech/neondb?sslmode=require

# ── NextAuth v5 ──────────────────────────────────────────────
# Generate: openssl rand -base64 32
# AUTH_SECRET is the canonical v5 name; keep NEXTAUTH_SECRET for fallback.
AUTH_SECRET=your-random-secret-here
NEXTAUTH_SECRET=your-random-secret-here

# Local dev only. On Vercel this is auto-detected — do NOT set it there
# unless you have a custom domain (then set to https://yourdomain.com).
AUTH_URL=http://localhost:3000
NEXTAUTH_URL=http://localhost:3000

# ── Google OAuth (optional) ─────────────────────────────────
# Get from: https://console.cloud.google.com → APIs & Services → Credentials
# Authorized redirect URIs must include:
#   http://localhost:3000/api/auth/callback/google
#   https://<your-vercel-subdomain>.vercel.app/api/auth/callback/google
#   https://yourdomain.com/api/auth/callback/google  (custom domain)
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret

# ── Cloudinary (image uploads) ──────────────────────────────
# Get from: https://cloudinary.com → Dashboard
CLOUDINARY_CLOUD_NAME=your-cloud-name
CLOUDINARY_API_KEY=your-api-key
CLOUDINARY_API_SECRET=your-api-secret

# ── Admin credentials ─────────────────────────────────────────
# Generate hash: node -e "require('bcryptjs').hash('YourPassword', 12).then(console.log)"
ADMIN_EMAIL=admin@yourdomain.com
ADMIN_PASSWORD_HASH=bcrypt-hash-of-your-password
ADMIN_NAME=Your Admin Name
